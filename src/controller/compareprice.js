import mongoose from 'mongoose';
import { Router } from 'express';
import schema from '../model/compareprice'
import bodyParser from 'body-parser';

export default({ config, db }) => {
    let api = Router();

    return api;
}